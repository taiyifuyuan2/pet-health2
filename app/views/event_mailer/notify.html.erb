<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <style>
      body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      .header { background-color: #007bff; color: white; padding: 20px; text-align: center; }
      .content { background-color: #f8f9fa; padding: 20px; }
      .event-details { background-color: white; padding: 20px; margin: 20px 0; border-radius: 8px; }
      .button { display: inline-block; background-color: #28a745; color: white; padding: 12px 24px; text-decoration: none; border-radius: 4px; margin: 10px 0; }
      .footer { text-align: center; color: #666; font-size: 12px; margin-top: 20px; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>ペット健康管理カレンダー</h1>
      </div>
      
      <div class="content">
        <h2>
          <% if @phase == :day %>
            本日の予定です
          <% else %>
            予定のお知らせです
          <% end %>
        </h2>
        
        <p>こんにちは、<%= @user.name %>さん</p>
        
        <div class="event-details">
          <h3><%= @event.title %></h3>
          <p><strong>対象:</strong> <%= @event.subject.name %></p>
          <p><strong>日時:</strong> 
            <%= @event.scheduled_on.strftime('%Y年%m月%d日') %>
            <% if @event.scheduled_time %>
              <%= @event.scheduled_time.strftime('%H:%M') %>
            <% end %>
          </p>
          <% if @event.note.present? %>
            <p><strong>メモ:</strong> <%= @event.note %></p>
          <% end %>
        </div>
        
        <p>
          <a href="<%= Rails.application.routes.url_helpers.complete_event_url(@event, host: 'localhost:3000') %>" class="button">
            完了する
          </a>
        </p>
        
        <p>
          <a href="<%= Rails.application.routes.url_helpers.dashboard_url(host: 'localhost:3000') %>">
            ダッシュボードを開く
          </a>
        </p>
      </div>
      
      <div class="footer">
        <p>このメールは自動送信されています。</p>
        <p>&copy; 2025 ペット健康管理カレンダー</p>
      </div>
    </div>
  </body>
</html>
